{
    "name": "json-server-app",
    "version": "1.0.0",
    "description": "JSON server app",
    "main": "server.js",
    "dependencies": {
      "json-server": "^0.16.3"
    },
    "scripts": {
      "start": "json-server --watch db.json --port 3000"
    }
  }